import { BaseStep } from "../core/quiz_base_steps/BaseStep";

export class DatePickerStep extends BaseStep {
  async complete(): Promise<void> {
    // I never saw this step again, so I couldn't find a way to complete it.
  }
}
